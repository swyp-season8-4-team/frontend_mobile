import 'package:flutter/material.dart';

class MyUserStoreListView extends StatelessWidget {
  const MyUserStoreListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
